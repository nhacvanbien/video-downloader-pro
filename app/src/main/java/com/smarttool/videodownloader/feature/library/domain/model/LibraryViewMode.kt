package com.smarttool.videodownloader.feature.library.domain.model

/** How the downloads list lays its rows out — a plain list, or a two-column tile grid. */
enum class LibraryViewMode {
    List,
    Grid,
}
