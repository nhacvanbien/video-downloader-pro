package com.smarttool.videodownloader.feature.language.domain

/** Was remote-config-driven alongside the ad experiment flags; now fixed at its default. */
const val NEW_UI_LFO = true

/** Was remote-config-driven alongside the ad experiment flags; now fixed at its default. */
const val TIME_APPLY_LFO_SECONDS = 4L
