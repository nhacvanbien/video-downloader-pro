package com.smarttool.videodownloader.feature.onboarding.domain.model

/** Where the app opens once the splash is done, decided by what the user has seen. */
enum class AppEntryPoint {
    Language,
    Intro,
    Home,
}
